import React from 'react';
import ReactDOM from 'react-dom';
import App from './frontend/App';

// Hier wird die App-Komponente in das HTML-Dokument gerendert
ReactDOM.render(<App />, document.getElementById('root'));
